services:
  phoenix-web:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - PORT=8000
      - DANCECOMP_DB_PATH=/data/dancecomp.db
    ports:
      - "8080:8000"
    volumes:
      - dancecomp-data:/data
    restart: unless-stopped

volumes:
  dancecomp-data:
